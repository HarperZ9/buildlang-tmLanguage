name: Release Artifacts

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Validate grammar files
        run: |
          python -m json.tool grammars/buildlang.tmLanguage.json > /dev/null
          python -m json.tool language-configuration.json > /dev/null
      - name: Build source package
        run: |
          python - <<'PY'
          from pathlib import Path
          from zipfile import ZIP_DEFLATED, ZipFile

          root = Path(".")
          dist = root / "dist"
          dist.mkdir(exist_ok=True)
          include_roots = [
              "grammars",
              "samples",
              "AUTHORS.md",
              "CHANGELOG.md",
              "CONTRIBUTING.md",
              "LICENSE",
              "README.md",
              "RELEASE.md",
              "language-configuration.json",
          ]
          with ZipFile(dist / "buildlang-tmLanguage-0.1.0.zip", "w", ZIP_DEFLATED) as zf:
              for item in include_roots:
                  path = root / item
                  if path.is_dir():
                      for file in path.rglob("*"):
                          if file.is_file():
                              zf.write(file, file.as_posix())
                  elif path.is_file():
                      zf.write(path, path.as_posix())
          PY
      - uses: actions/upload-artifact@v5
        with:
          name: buildlang-tmLanguage-source-package
          path: dist/buildlang-tmLanguage-0.1.0.zip
